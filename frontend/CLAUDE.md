@AGENTS.md

Read `../../CLAUDE.md` for project-wide conventions.

Frontend module (Mateusz only). Demo UI for the Process-to-Automation Copilot. Talks to FastAPI backend at `http://localhost:8000`. Stack: Next.js + shadcn/ui + Tailwind. Dark mode default. Geist fonts. All data from backend API, not Next.js API routes.
